<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.DOHApprovedHerbalPlantIdentifcation">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.App.Starting"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NextActivity"
            android:exported="true" />

        <activity
            android:name=".Category"
            android:exported="true"
            android:theme="@style/TransparentTheme" />

        <activity android:name=".Classify"
            android:exported="true"
            android:theme="@style/TransparentTheme" />

        <activity android:name=".HerbActivity"
            android:exported="true"
            android:theme="@style/TransparentTheme" />

        <activity android:name=".ScannerActivity"
            android:exported="true"
            android:theme="@style/TransparentTheme" />

        <activity android:name=".AboutUs"
            android:theme="@style/TransparentTheme"/>

        <activity android:name=".Setting"
            android:theme="@style/TransparentTheme"/>

        <meta-data
            android:name="developer_credit"
            android:value="Programmed by John Dupal" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.greenbuddy.doh_approvedherb_identifier.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>